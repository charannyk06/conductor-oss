{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "dev:full",
      "runtimeExecutable": "bun",
      "runtimeArgs": ["run", "dev:full"],
      "port": 3000,
      "description": "Start both frontend (port 3000) and backend (port 4749)"
    },
    {
      "name": "dev:frontend",
      "runtimeExecutable": "bun",
      "runtimeArgs": ["run", "dev"],
      "port": 3000,
      "description": "Start frontend only (port 3000) - requires separate backend"
    },
    {
      "name": "dev:backend",
      "runtimeExecutable": "bun",
      "runtimeArgs": ["run", "dev:backend"],
      "port": 4749,
      "description": "Start backend only (port 4749) - requires separate frontend"
    }
  ]
}
